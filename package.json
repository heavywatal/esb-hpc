{
  "name": "esb-hpc",
  "version": "1.0.0",
  "scripts": {
    "stop": "ls -d public/* | grep -Ev '.git' | xargs rm -r",
    "test": "hugo -D",
    "build": "hugo --destination=public",
    "start": "hugo --destination=public -w"
  }
}
